<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipToPadding="false"
    android:padding="16dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/toast_card1"
        android:layout_width="24dp"
        android:layout_height="24dp"
        app:cardBackgroundColor="@color/blue728"
        app:cardCornerRadius="12dp"
        app:layout_constraintEnd_toStartOf="@+id/toast_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/toast_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:text="Я тостер"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/toast_card2"
        app:layout_constraintStart_toEndOf="@+id/toast_card1"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.cardview.widget.CardView
        android:id="@+id/toast_card2"
        android:layout_width="24dp"
        android:layout_height="24dp"
        app:cardBackgroundColor="@color/blue728"
        app:cardCornerRadius="12dp"
        app:layout_constraintStart_toEndOf="@+id/toast_text"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>